export interface IFocusData {
  command: string;
  block: string;
  field: string;
}

export interface IFocusGetData {
  block: string;
  focus: string;
}

export interface IBlurGetData {
  block: string;
  blur: string;
}